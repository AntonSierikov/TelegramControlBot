using System;

namespace ControlBot.BL.Messages
{
    public class GeneralMessage
    {
        public const String TIME_INCORRECT_FORMAT = "Time has incorrect format";

        public const String COMMAND_NOT_MATCH_PATTERN = "Command isn't matched for pattern";

        public const String DATE_INCORRECT_FORMAT = "Date has incorrect format";
    }
}
